import { useState } from 'react';
import { Table } from '@ostore/table';

function App() {
  const [count, setCount] = useState(0)

  return (
    <div>
      ss
      <Table />
    </div>
  )
}

export default App
